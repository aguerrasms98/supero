CountDipendenti = var datee = max('Calendar'[Date])
return
calculate(
    DISTINCTCOUNT(RapportoLavoro[CodSoggetto]),
    filter(
        RapportoLavoro,
        datee >= RapportoLavoro[DataInizioValidita] && datee <= RapportoLavoro[DataFineValidita]
    )
)
